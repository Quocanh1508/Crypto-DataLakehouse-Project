export declare const HNINDEX_IGNORE_FILENAME = ".hnindexignore";
/**
 * Parse .hnindexignore body: blank lines and # comments skipped; no `!` negation in v1.
 */
export declare function parseHnindexIgnoreContent(content: string): string[];
export declare function loadHnindexIgnore(rootPath: string): string[];
/**
 * Gitignore-like matching: patterns with `/` are relative to root; patterns without `/`
 * match any path segment (also under subdirs).
 */
export declare function matchesHnindexPattern(relativePath: string, pattern: string): boolean;
export declare function isIgnored(relativePath: string, patterns: string[]): boolean;
