import type { SymbolKind, SymbolRecord } from '../types.js';
export interface ParsedSymbol {
    name: string;
    kind: SymbolKind;
    lineNumber: number;
    signature: string | null;
    parentName: string | null;
    exported: boolean;
}
/** Heuristic symbol extraction (no full AST). TS/JS + Python first. */
export declare function extractSymbols(content: string, language: string): ParsedSymbol[];
export declare function toSymbolRecords(projectName: string, filePath: string, language: string, parsed: ParsedSymbol[]): SymbolRecord[];
