export interface ParsedImport {
    specifier: string;
    specifiers: string[] | null;
    importType: 'static' | 'dynamic' | 'require' | 'type-only';
}
export interface ParsedExport {
    name: string;
    exportType: 'function' | 'class' | 'variable' | 'type' | 'interface' | 'enum' | 'default';
    lineNumber: number;
}
export declare function parseImports(content: string, language: string): ParsedImport[];
export declare function parseExports(content: string, language: string): ParsedExport[];
export declare function resolveImportPath(specifier: string, sourceFile: string, projectFiles: string[]): string | null;
