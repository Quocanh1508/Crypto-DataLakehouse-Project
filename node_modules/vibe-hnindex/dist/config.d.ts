export declare const config: {
    readonly ollamaUrl: string;
    readonly embeddingModel: string;
    readonly embeddingDimensions: 1024;
    readonly storagePath: string;
    readonly sqlitePath: string;
    readonly qdrantUrl: string;
    readonly qdrantApiKey: string | undefined;
    readonly qdrantCollectionPrefix: string;
    readonly chunkSize: number;
    readonly chunkOverlap: number;
    readonly maxFileSize: number;
    readonly embeddingBatchSize: 32;
    /** When true and `mode` is omitted, behave like `mode: auto` (heuristic keyword/hybrid). */
    readonly searchAutoRoute: boolean;
    /** When keyword mode returns no hits, run semantic once if Ollama+Qdrant are OK. */
    readonly searchKeywordFallbackSemantic: boolean;
    /** Rerank top results after hybrid/semantic path scoring (HTTP and/or semantic reorder). */
    readonly searchRerankEnabled: boolean;
    /** Max distinct files before rerank trim (then cut to `limit`). */
    readonly searchRerankPool: number;
    /** POST JSON `{ query, documents }` → `{ scores: number[] }`. Same length as documents. */
    readonly rerankUrl: string;
    readonly rerankTimeoutMs: number;
};
export declare function getCollectionName(projectName: string): string;
