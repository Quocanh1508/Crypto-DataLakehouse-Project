/** Plain markdown section for onboarding / composition (no MCP wrapper). */
export declare function formatProjectStatsSection(projectName: string): string | null;
export declare function projectStatsTool(args: {
    project_name: string;
}): Promise<{
    content: Array<{
        type: 'text';
        text: string;
    }>;
}>;
