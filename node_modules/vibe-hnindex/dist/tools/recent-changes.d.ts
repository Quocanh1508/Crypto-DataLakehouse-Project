/** Compact markdown for onboarding (subset of recentChangesTool). */
export declare function formatRecentChangesSection(projectName: string, days: number, limit: number): Promise<string | null>;
export declare function recentChangesTool(args: {
    project_name: string;
    days?: number;
    limit?: number;
}): Promise<{
    content: Array<{
        type: 'text';
        text: string;
    }>;
}>;
