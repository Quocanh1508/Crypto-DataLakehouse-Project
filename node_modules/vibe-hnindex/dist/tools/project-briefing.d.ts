import type { ProjectInfo } from '../types.js';
export declare function buildBriefingCacheKey(project: ProjectInfo): string;
export declare function buildMarkdownBriefing(projectName: string): string;
export declare function getOrBuildBriefingMarkdown(projectName: string, regenerate: boolean): {
    markdown: string;
    fromCache: boolean;
};
export declare function projectBriefingTool(args: {
    project_name: string;
    regenerate?: boolean;
}): Promise<{
    content: Array<{
        type: 'text';
        text: string;
    }>;
}>;
