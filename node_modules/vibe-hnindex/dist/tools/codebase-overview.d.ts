export declare function codebaseOverviewTool(args: {
    project_name: string;
}): Promise<{
    content: Array<{
        type: 'text';
        text: string;
    }>;
}>;
export declare function buildDirTree(files: string[]): string;
export declare function detectEntryPoints(files: string[]): string[];
export declare function detectFrameworks(projectName: string, files: string[]): string[];
